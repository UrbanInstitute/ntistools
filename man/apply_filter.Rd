% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_filter.R
\name{apply_filter}
\alias{apply_filter}
\title{Conditionally set variables to NA when a condition is not met}
\usage{
apply_filter(data, vars, condition)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{A character vector of column names to filter.}

\item{condition}{An unquoted expression that evaluates to a logical vector.
Rows where this is TRUE keep their values; all other rows get NA.}
}
\value{
\code{data} with filtered columns.
}
\description{
For each variable in \code{vars}, values are kept as-is when \code{condition} is TRUE
and set to NA otherwise. This is useful for filtering out responses from
organizations that should not answer certain questions.
}
\examples{
d <- data.frame(keep = c(1, 0, 1), x = c(10, 20, 30))
apply_filter(d, "x", condition = keep == 1)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_summarize.R
\name{calc_summarize}
\alias{calc_summarize}
\title{Calculate weighted summary statistics}
\usage{
calc_summarize(svy_df, var, wt_var, grp_cols, metric)
}
\arguments{
\item{svy_df}{A data frame containing the survey data.}

\item{var}{A string naming the variable to summarize.}

\item{wt_var}{A string naming the survey weight column (e.g., \code{"year5wt"}).}

\item{grp_cols}{Character vector of grouping columns. The first element is
treated as the main grouping variable; the last as the subgroup. For a
national (ungrouped) calculation, pass \code{var} as a length-1 vector
(i.e., \code{grp_cols = var}).}

\item{metric}{One of \code{"proportion"}, \code{"mean"}, or \code{"median"}.}
}
\value{
A tibble with columns \code{group}, \code{variable}, optionally
\code{group_level} and \code{variable_level}, \code{count} (sum of weights), \code{value}
(the calculated statistic), and \code{metric}.
}
\description{
Calculates weighted proportions, means, or medians for a variable, optionally
within levels of a grouping variable.
}
\details{
Rows where any column in \code{grp_cols} is \code{NA} are dropped before
summarising. The median branch uses an internal weighted-median helper to
avoid taking on a \code{matrixStats} dependency.
}
\examples{
d <- data.frame(
  LLTwoDisruption = c(0, 1, 1, 0, 1, 0),
  LLLost = c(1, 0, 1, 0, 0, 1),
  year5wt = c(100, 150, 200, 100, 50, 150),
  Q_A = c(1, 2, 1, 2, 1, 2)
)
calc_summarize(d, var = "Q_A", wt_var = "year5wt",
               grp_cols = c("LLTwoDisruption", "Q_A"),
               metric = "proportion")
calc_summarize(d, var = "LLLost", wt_var = "year5wt",
               grp_cols = "LLTwoDisruption", metric = "mean")
}

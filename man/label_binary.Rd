% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_binary.R
\name{label_binary}
\alias{label_binary}
\title{Label binary columns with descriptive strings}
\usage{
label_binary(data, labels, true_values = 1, false_values = 0, na_values = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{labels}{A named list where each element is a length-2 character vector
\code{c("true label", "false label")}, keyed by column name.}

\item{true_values}{Numeric vector of values that map to the true (first)
label. Defaults to \code{1}.}

\item{false_values}{Numeric vector of values that map to the false (second)
label. Defaults to \code{0}.}

\item{na_values}{Optional numeric vector of additional values to map to the
false label (e.g., \code{97} for "not applicable"). Defaults to \code{NULL}.}
}
\value{
\code{data} with labeled columns.
}
\description{
Converts 0/1 columns into character columns using named label pairs.
Values in \code{true_values} get the first label, values in \code{false_values} get
the second label, and everything else becomes \code{NA_character_}.
}
\examples{
d <- data.frame(x = c(1, 0, NA, 97))
label_binary(d,
  labels = list(x = c("Yes", "No")),
  na_values = 97
)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_anova.R
\name{survey_anova}
\alias{survey_anova}
\title{Weighted ANOVA across multiple outcomes}
\usage{
survey_anova(
  svy_df,
  outcomes,
  group_var,
  wt_var,
  pairwise = FALSE,
  adjust = "bonferroni"
)
}
\arguments{
\item{svy_df}{A data frame containing the survey data.}

\item{outcomes}{Character vector of (continuous) outcome variables.}

\item{group_var}{A string naming the categorical grouping variable. Will be
coerced to factor.}

\item{wt_var}{A string naming the survey weight column.}

\item{pairwise}{If \code{TRUE}, also return pairwise contrasts from
\code{emmeans::emmeans()}.}

\item{adjust}{Multiple-comparison adjustment for pairwise contrasts. See
\code{?emmeans::summary.emmGrid}.}
}
\value{
A list with element \code{overall} (one row per outcome: \code{outcome},
\code{group_var}, \code{statistic}, \code{df_num}, \code{df_den}, \code{p_value}) and, if
\code{pairwise = TRUE}, element \code{pairwise} (one row per contrast: \code{outcome},
\code{contrast}, \code{estimate}, \code{se}, \code{df}, \code{t_ratio}, \code{p_value}).
}
\description{
Fits \code{survey::svyglm()} for each outcome against a categorical grouping
variable and runs \code{survey::regTermTest()}. Optionally returns pairwise
contrasts via \code{emmeans}.
}
\details{
Requires the \code{survey} package. Pairwise contrasts also require \code{emmeans}.
}
\examples{
\dontrun{
survey_anova(nptrends_y6_clean,
             outcomes = "LLLostAmt",
             group_var = "SizeStrata",
             wt_var = "weight_year6plus",
             pairwise = TRUE)
}
}

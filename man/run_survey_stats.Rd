% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_survey_stats.R
\name{run_survey_stats}
\alias{run_survey_stats}
\title{Run a bulk batch of weighted survey statistics}
\usage{
run_survey_stats(
  datasets,
  ci_spec = NULL,
  chisq_spec = NULL,
  ttest_spec = NULL,
  anova_spec = NULL,
  output_dir = NULL,
  combined = FALSE
)
}
\arguments{
\item{datasets}{Named list of data frames. Spec rows reference datasets by
name via a \code{dataset} column.}

\item{ci_spec}{Optional data frame of CI jobs. Required columns: \code{dataset},
\code{variable}, \code{wt_var}. Optional: \code{group_var}.}

\item{chisq_spec}{Optional data frame of chi-square jobs. Required columns:
\code{dataset}, \code{var1}, \code{var2}, \code{wt_var}.}

\item{ttest_spec}{Optional data frame of t-test jobs. Required columns:
\code{dataset}, \code{outcome}, \code{group_var}, \code{wt_var}.}

\item{anova_spec}{Optional data frame of ANOVA jobs. Required columns:
\code{dataset}, \code{outcome}, \code{group_var}, \code{wt_var}. Optional: \code{pairwise}
(logical).}

\item{output_dir}{Directory to write CSVs to. If \code{NULL}, results are not
written; only returned.}

\item{combined}{If \code{TRUE}, write a single \code{survey_stats.csv} with an
\code{analysis} column instead of one CSV per analysis type. Pairwise ANOVA
contrasts are always written separately when present.}
}
\value{
A named list of result data frames (\code{ci}, \code{chisq}, \code{ttest},
\code{anova_overall}, optionally \code{anova_pairwise}). The same data is written to
disk when \code{output_dir} is supplied.
}
\description{
Drives \code{\link[=survey_ci]{survey_ci()}}, \code{\link[=survey_chisq]{survey_chisq()}}, \code{\link[=survey_ttest]{survey_ttest()}}, and
\code{\link[=survey_anova]{survey_anova()}} over one or more datasets and writes the results to CSV.
Each spec is a data frame describing the jobs for that analysis type;
results from all jobs of a given type are stacked into one CSV (or merged
into a single combined CSV if \code{combined = TRUE}).
}
\examples{
\dontrun{
run_survey_stats(
  datasets = list(y6 = nptrends_y6_clean),
  ci_spec = data.frame(
    dataset = "y6",
    variable = c("LLAnyDisruption", "LLLost"),
    wt_var = "weight_year6plus",
    group_var = "SizeStrata"
  ),
  chisq_spec = data.frame(
    dataset = "y6",
    var1 = "SizeStrata", var2 = "LLAnyDisruption",
    wt_var = "weight_year6plus"
  ),
  output_dir = "stats_out"
)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_likert.R
\name{label_likert}
\alias{label_likert}
\title{Label Likert-scale columns with descriptive strings}
\usage{
label_likert(
  data,
  vars,
  labels,
  mapping = c(1L, 1L, 2L, 3L, 3L),
  na_values = 97,
  na_label = "Unsure"
)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{A character vector of column names to label.}

\item{labels}{A character vector of labels, one per collapsed category.
Length must equal \code{max(mapping)}. For example,
\code{c("Decrease", "No change", "Increase")}.}

\item{mapping}{An integer vector where position i gives the collapsed
category for original value i. Defaults to \code{c(1L, 1L, 2L, 3L, 3L)}.}

\item{na_values}{Numeric vector of values to map to \code{na_label}.
Defaults to \code{97}.}

\item{na_label}{Character string used for values in \code{na_values}.
Defaults to \code{"Unsure"}.}
}
\value{
\code{data} with labeled columns.
}
\description{
Maps numeric Likert-scale values to descriptive labels using the same
\code{mapping} vector approach as \code{\link[=collapse_likert]{collapse_likert()}}, then indexes into \code{labels}
to produce character output. Values in \code{na_values} are mapped to \code{na_label}
instead of \code{NA}.
}
\examples{
d <- data.frame(q1 = c(1, 2, 3, 4, 5, 97))
label_likert(d, "q1",
  labels = c("Decrease", "No change", "Increase")
)
}
